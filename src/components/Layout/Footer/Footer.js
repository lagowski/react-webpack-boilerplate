import React from "react";
import PropTypes from "prop-types";

function Footer() {
    return (
        <footer>
            FOOTER
        </footer>
    );
}

Footer.propTypes = {
    white: PropTypes.bool,
};

export default Footer;
